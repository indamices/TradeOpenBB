fastapi
uvicorn[standard]
sqlalchemy
pydantic==2.10.5
pydantic-core==2.27.2
pandas
numpy
yfinance
requests
openpyxl>=3.1.0  # For Excel export
futu==0.0.1  # Futu OpenAPI Python SDK for connecting to OpenD client
python-dotenv
cryptography
cachetools
typing-extensions
# Testing dependencies
pytest
pytest-asyncio
httpx
pytest-benchmark
pytest-cov
# PostgreSQL support (required for persistent storage)
psycopg2-binary
# Optional: OpenBB (can be installed later if needed)
# openbb-terminal
# Optional: AI providers (install as needed)
openai  # Required for OpenAI models
# anthropic  # Uncomment if using Claude
# google-genai  # Uncomment if using Gemini
# Task scheduler for background jobs
apscheduler  # Optional, for scheduled data sync tasks