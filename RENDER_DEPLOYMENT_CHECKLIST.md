# Render 部署检查清单 ✅

## 📋 部署前检查

### ✅ 文件结构
- [x] `render.yaml` 在仓库根目录
- [x] `backend/Dockerfile` 存在且配置正确
- [x] `backend/requirements.txt` 包含所有依赖
- [x] `package.json` 在根目录
- [x] `vite.config.ts` 配置正确
- [x] `index.html` 和 `index.tsx` 在根目录

### ✅ 配置文件
- [x] `render.yaml` 路径配置正确：
  - `dockerfilePath: ./backend/Dockerfile`
  - `dockerContext: ./backend`
  - `buildCommand: npm install && npm run build`
  - `staticPublishPath: ./dist`
- [x] `Dockerfile` 使用生产模式（无 `--reload`）
- [x] CORS 配置包含 Render 域名
- [x] 数据库配置支持 SQLite（默认）和 PostgreSQL

### ✅ 代码检查
- [x] 前端使用 `import.meta.env.VITE_API_BASE_URL`
- [x] `vite.config.ts` 定义了 `VITE_API_BASE_URL`
- [x] 后端导入路径正确（支持相对和绝对导入）
- [x] API 客户端配置正确
- [x] 无硬编码路径

### ✅ 环境变量
- [x] `DATABASE_URL` 在 render.yaml 中配置（SQLite 默认）
- [x] `ENCRYPTION_KEY` 设置为自动生成
- [x] `VITE_API_BASE_URL` 自动从后端服务获取
- [x] 可选变量已标记为 `sync: false`

## 🚀 部署步骤

### 1. 推送到 GitHub
```bash
git add .
git commit -m "Prepare for Render deployment"
git push origin main
```

### 2. 在 Render 创建服务
1. 访问 https://render.com
2. 登录（使用 GitHub 账户）
3. 点击 "New +" → "Blueprint"
4. 连接仓库：`indamices/TradeOpenBB`
5. 点击 "Apply"

### 3. 等待部署
- 后端：5-8 分钟
- 前端：3-5 分钟

### 4. 配置环境变量（可选）
- `GEMINI_API_KEY` - 如果使用 Gemini AI
- `API_KEY` - 如果使用外部 API

### 5. 验证部署
- [ ] 后端健康检查：`https://tradeopenbb-backend.onrender.com/`
- [ ] API 文档：`https://tradeopenbb-backend.onrender.com/docs`
- [ ] 前端访问：`https://tradeopenbb-frontend.onrender.com`
- [ ] 前端可以连接后端（检查浏览器控制台）

## 🔧 故障排除

### 问题 1: Render 找不到 render.yaml
**解决**: 确保 `render.yaml` 在仓库根目录，不在子目录中

### 问题 2: 构建失败
**检查**:
- 查看构建日志
- 检查 `requirements.txt` 和 `package.json`
- 验证 Dockerfile 语法

### 问题 3: 前端无法连接后端
**检查**:
- `VITE_API_BASE_URL` 环境变量
- 后端服务状态（应该是 "Live"）
- CORS 配置

### 问题 4: 数据库连接失败
**检查**:
- `DATABASE_URL` 环境变量
- 如果使用 SQLite，确保路径正确
- 如果使用 PostgreSQL，确保数据库服务运行

## 📊 部署后验证清单

- [ ] 后端服务状态为 "Live"
- [ ] 前端服务状态为 "Live"
- [ ] 健康检查端点返回成功
- [ ] API 文档可以访问
- [ ] 前端页面可以正常加载
- [ ] 前端可以创建投资组合
- [ ] 前端可以添加持仓
- [ ] API 请求成功（无 CORS 错误）

## 📝 重要提示

1. **免费层限制**:
   - 后端服务 15 分钟无活动后会休眠
   - 首次请求需要 30-60 秒唤醒
   - 这是正常的，不影响功能

2. **环境变量**:
   - 敏感信息使用环境变量
   - 不要硬编码 API Key

3. **更新部署**:
   - 推送代码到 GitHub 会自动触发重新部署
   - 或手动点击 "Manual Deploy"

---

**所有检查项已完成！可以开始部署了！** 🚀
